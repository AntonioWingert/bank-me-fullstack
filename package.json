{
  "devDependencies": {
    "husky": "^9.0.11"
  },
  "scripts": {
    "prepare": "husky",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f"
  },
  "lint-staged": {
    "frontend/**/*.js": "eslint --fix",
    "frontend/**/*.ts": "eslint --fix",
    "backend/**/*.js": "eslint --fix",
    "backend/**/*.ts": "eslint --fix"
  },
  "dependencies": {
    "eslint": "^9.3.0",
    "lint-staged": "^15.2.4"
  }
}
